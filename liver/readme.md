## 试验
1. 直接load dicom和load其它数据格式的时间差
2. mask数据的数据格式
   1. 数据格式
   2. 数据类别（数量）
3. 同一序列的mask标签是否是互斥的，如果是，可以将所有分割融合到同一个模型中
   1. mask值与标签的对应关系
   2. 标签间是否彼此互斥，能否直接融合
4. 将mask标签转换为npy格式进行存储，以加快读写速度
5. mask数据做不同resize时，肉眼观察mask的分辨率
6. 做不同crop时，mask肉眼观察的效果
7. itk_snap能否同时显示volume和mask，观察并纪律显示效果(可以)
8. 统计数据的最大和最小层数，并根据相应需求，提前做好padding，保证数据预处理步骤更快
9. 实现并测试data generator的效果
10. 
## model 

## data
